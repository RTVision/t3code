name: RTVision release

on:
  push:
    branches: [rtvision]
    paths:
      - apps/server/package.json
      - .github/workflows/rtvision-release.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rtvision-release
  cancel-in-progress: false

env:
  T3CODE_DESKTOP_UPDATE_REPOSITORY: RTVision/t3code

jobs:
  prepare:
    if: github.repository == 'RTVision/t3code' && github.ref == 'refs/heads/rtvision'
    runs-on: ubuntu-24.04
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v6
      - id: version
        run: |
          node -e 'const fs = require("fs"); const { version } = require("./apps/server/package.json"); if (!/^\d+\.\d+\.\d+$/.test(version)) throw Error("RTVision releases require a stable version"); fs.appendFileSync(process.env.GITHUB_OUTPUT, `version=${version}\n`);'
      - name: Refuse to replace an existing release tag
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          existing=$(git ls-remote origin "refs/tags/v$VERSION")
          if [ -n "$existing" ]; then
            echo "v$VERSION already exists. Bump the release version before building."
            exit 1
          fi

  linux-cli:
    needs: prepare
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
      - name: Enable production T3 Connect
        run: cp .env.example .env
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version-file: package.json
          run-install: |
            args:
              - --frozen-lockfile
              - --filter=t3...
              - --filter=@t3tools/scripts...
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu
      - run: vp run --filter t3 build
      - name: Build Linux CLI executable
        env:
          VP_NODE_VERSION: "26.8.2"
        run: node apps/server/scripts/cli.ts build-exe --verbose
      - name: Build Linux resource monitor
        run: |
          cargo build --locked --release --manifest-path native/resource-monitor/Cargo.toml --target x86_64-unknown-linux-gnu
          mkdir -p resource-monitor/linux-x64
          cp native/resource-monitor/target/x86_64-unknown-linux-gnu/release/t3-resource-monitor resource-monitor/linux-x64/
      - name: Build and verify Linux CLI archive
        run: |
          node scripts/build-cli-archive.ts --platform linux --arch x64 --version "${{ needs.prepare.outputs.version }}" --resource-monitor-dir resource-monitor --output-dir release-cli
          node scripts/smoke-cli-archive.ts --archive release-cli/* --expect-version "${{ needs.prepare.outputs.version }}"
      - uses: actions/upload-artifact@v7
        with:
          name: rtvision-cli-linux-x64
          path: release-cli/*
          if-no-files-found: error

  desktop:
    needs: [prepare, linux-cli]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            platform: linux
            arch: x64
            target: AppImage
            rust: x86_64-unknown-linux-gnu
            resource: linux-x64
          - runner: windows-2025
            platform: win
            arch: x64
            target: nsis
            rust: x86_64-pc-windows-msvc
            resource: win32-x64
    steps:
      - uses: actions/checkout@v6
      - name: Enable production T3 Connect
        shell: bash
        run: cp .env.example .env
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version-file: package.json
          run-install: false
      - name: Install dependencies
        run: vp install --frozen-lockfile --filter=@t3tools/desktop... --filter=t3... --filter=@t3tools/scripts...
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust }}
      - name: Install Linux build libraries
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libsecret-1-dev pkg-config imagemagick
      - name: Install Windows Spectre-mitigated MSVC libraries
        if: matrix.platform == 'win'
        shell: pwsh
        run: |
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $installPath = & $vswhere -products * -latest -property installationPath
          $setup = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\setup.exe"
          $proc = Start-Process -FilePath $setup -ArgumentList "modify", "--installPath", "`"$installPath`"", "--add", "Microsoft.VisualStudio.Component.VC.Runtimes.x86.x64.Spectre", "--quiet", "--norestart" -Wait -PassThru -NoNewWindow
          if ($proc.ExitCode -notin @(0, 3010)) { throw "Visual Studio Installer failed: $($proc.ExitCode)" }
      - uses: actions/download-artifact@v8
        if: matrix.platform == 'win'
        with:
          name: rtvision-cli-linux-x64
          path: wsl-runtime
      - name: Build desktop installer
        shell: bash
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          PLATFORM: ${{ matrix.platform }}
          TARGET: ${{ matrix.target }}
          ARCH: ${{ matrix.arch }}
        run: |
          args=(--platform "$PLATFORM" --target "$TARGET" --arch "$ARCH" --build-version "$VERSION" --verbose)
          if [ "$PLATFORM" = win ]; then
            args+=(--wsl-runtime "$GITHUB_WORKSPACE/wsl-runtime/t3-$VERSION-linux-x64.tar.gz")
          fi
          vp run dist:desktop:artifact "${args[@]}"
      - name: Collect installers and updater metadata
        shell: bash
        env:
          PLATFORM: ${{ matrix.platform }}
          ARCH: ${{ matrix.arch }}
          RUST_TARGET: ${{ matrix.rust }}
        run: |
          mkdir -p release-publish resource-monitor
          shopt -s nullglob
          for file in release/*.dmg release/*.zip release/*.AppImage release/*.exe release/*.blockmap release/*.yml; do
            cp "$file" release-publish/
          done
          binary=t3-resource-monitor
          if [ "$PLATFORM" = win ]; then binary=$binary.exe; fi
          cp "native/resource-monitor/target/$RUST_TARGET/release/$binary" resource-monitor/
      - uses: actions/upload-artifact@v7
        with:
          name: rtvision-desktop-${{ matrix.platform }}-${{ matrix.arch }}
          path: release-publish/*
          if-no-files-found: error
      - uses: actions/upload-artifact@v7
        with:
          name: rtvision-resource-${{ matrix.resource }}
          path: resource-monitor/*
          if-no-files-found: error

      - name: Build Windows CLI executable
        if: matrix.platform == 'win'
        shell: bash
        env:
          VP_NODE_VERSION: "26.8.2"
        run: node apps/server/scripts/cli.ts build-exe --verbose
      - name: Build and verify Windows CLI archive
        if: matrix.platform == 'win'
        shell: bash
        run: |
          mkdir -p cli-resource-monitor/win32-x64
          cp resource-monitor/t3-resource-monitor.exe cli-resource-monitor/win32-x64/
          node scripts/build-cli-archive.ts --platform win --arch x64 --version "${{ needs.prepare.outputs.version }}" --resource-monitor-dir cli-resource-monitor --output-dir release-cli
          node scripts/smoke-cli-archive.ts --archive release-cli/* --expect-version "${{ needs.prepare.outputs.version }}"
      - uses: actions/upload-artifact@v7
        if: matrix.platform == 'win'
        with:
          name: rtvision-cli-win32-x64
          path: release-cli/*
          if-no-files-found: error

  cli:
    needs: [prepare, desktop]
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - name: Enable production T3 Connect
        run: cp .env.example .env
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version-file: package.json
          run-install: |
            args:
              - --frozen-lockfile
              - --filter=t3...
              - --filter=@t3tools/scripts...
      - run: vp run --filter t3 build
      - uses: actions/download-artifact@v8
        with:
          pattern: rtvision-resource-*
          path: resource-monitors
      - name: Pack CLI with resource monitors
        run: |
          for directory in resource-monitors/rtvision-resource-*; do
            key=${directory##*/rtvision-resource-}
            mkdir -p "apps/server/dist/resource-monitor/$key"
            cp "$directory"/* "apps/server/dist/resource-monitor/$key/"
            chmod +x "apps/server/dist/resource-monitor/$key/"* || true
          done
          mkdir -p release-npm
          node apps/server/scripts/cli.ts publish --pack-destination release-npm --verbose
      - uses: actions/upload-artifact@v7
        with:
          name: rtvision-npm
          path: release-npm/*.tgz
          if-no-files-found: error

  draft:
    needs: [prepare, desktop, cli]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v8
        with:
          pattern: rtvision-desktop-*
          merge-multiple: true
          path: release-assets
      - uses: actions/download-artifact@v8
        with:
          name: rtvision-npm
          path: release-assets
      - uses: actions/download-artifact@v8
        with:
          pattern: rtvision-cli-*
          merge-multiple: true
          path: release-assets
      - name: Create draft for local npm publication
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          (cd release-assets && sha256sum * > SHA256SUMS)
          cat > "$RUNNER_TEMP/release-notes.md" <<'NOTES'
          RTVision T3 Code uses `@rtvision/t3` from `https://npm-registry.rtvision.com/` and desktop updates from this repository.

          Desktop installers are unsigned. Install only on machines that can reach RTVision's npm registry when using server installation or update features.

          Maintainers: publish the attached Node-based npm tarball to RTVision's registry, verify its version and integrity, then publish this draft. See `docs/operations/rtvision-release.md`.
          NOTES
          gh release create "v$VERSION" release-assets/* --target "$GITHUB_SHA" --title "RTVision T3 Code $VERSION" --notes-file "$RUNNER_TEMP/release-notes.md" --draft
